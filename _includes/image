{% comment %}

This is used for quickly include image

Usage:
    {% include image path="<path>" pos="fit" %}

pos = fit | left | right | main (default is fit)

{% if include.pos %}
<span class="image {{ include.pos }}"><img src="{{ site.baseurl }}{{ include.path }}" alt="" /></span>
{% else %}
<span class="image fit"><img src="{{ site.baseurl }}{{ include.path }}" alt="" /></span>
{% endif %}
{% endcomment %}
